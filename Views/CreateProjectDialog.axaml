<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:ProjectPlan.ViewModels"
        x:Class="ProjectPlan.Views.CreateProjectDialog"
        x:DataType="vm:CreateProjectDialogViewModel"
        Width="520"
        Height="520"
        CanResize="False"
        Background="#EEF0F2"
        Title="Créer un projet">

  <Border Padding="16">
    <DockPanel LastChildFill="True">
      <DockPanel DockPanel.Dock="Bottom" Margin="0,8,0,0" LastChildFill="False">
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="8">
          <Button Click="Cancel_Click">Annuler</Button>
          <Button Click="Create_Click">Valider</Button>
        </StackPanel>
      </DockPanel>

      <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="12">
          <TextBlock FontSize="20" FontWeight="Bold" Text="Créer un projet" />

          <StackPanel Spacing="6">
            <TextBlock Text="Titre" />
            <TextBox Text="{Binding ProjectTitle}" Watermark="Ex: Mon projet" />
          </StackPanel>

          <StackPanel Spacing="6">
            <TextBlock Text="Description" />
            <TextBox Text="{Binding ProjectDescription}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="110"
                     Watermark="Décris rapidement le projet..." />
          </StackPanel>

          <StackPanel Spacing="6">
            <TextBlock Text="Image" />
            <DockPanel LastChildFill="True">
              <Button DockPanel.Dock="Left" Width="150" Click="ChooseImage_Click">Choisir...</Button>
              <TextBlock Margin="10,0,0,0"
                         VerticalAlignment="Center"
                         TextTrimming="CharacterEllipsis"
                         Text="{Binding ImagePath}" />
            </DockPanel>

            <Border Background="#FFFFFF" BorderThickness="1" CornerRadius="8" Padding="8">
              <Image Height="160" Source="{Binding ImagePreview}" Stretch="Uniform" />
            </Border>
          </StackPanel>
        </StackPanel>
      </ScrollViewer>
    </DockPanel>
  </Border>
</Window>
